# Discord AutoRemove Bot

## Zum Verwenden

1. Installiere die Benötigten Dependencies mit `npm i`
2. Mache eine `.env` Datei mit `mongouri` und `discordtoken`
3. Starte den Server mit `node start`

## Benutze es

Die Commands sind `auto.start` und `auto.stop`
